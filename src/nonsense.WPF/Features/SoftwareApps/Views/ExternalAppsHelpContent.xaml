<UserControl x:Class="nonsense.WPF.Features.SoftwareApps.Views.ExternalAppsHelpContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d">
    
    <!-- Flyout Help Content -->
    <Border Background="{DynamicResource WindowBackground}"
            BorderBrush="#505050"
            BorderThickness="1"
            CornerRadius="4"
            Padding="15,10"
            MinWidth="400"
            MaxWidth="500"
            MaxHeight="300"
            Effect="{StaticResource LightShadowEffect}">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0"
                               Text="External Apps Help"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource PrimaryTextColor}"
                               VerticalAlignment="Center"/>
                    
                    <Button Grid.Column="1"
                            x:Name="CloseButton"
                            Style="{StaticResource FlyOutCloseButtonStyle}"
                            ToolTip="Close Help"
                            Command="{Binding CloseHelpCommand}">
                        <iconPacks:PackIconMaterial Kind="Close" Width="12" Height="12"/>
                    </Button>
                </Grid>
            
                <!-- Info Text -->
                <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Info Text -->
                <TextBlock Grid.Column="1"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Margin="0,10,0,10"
                           FontSize="13"
                           TextWrapping="Wrap"
                           Foreground="{DynamicResource PrimaryTextColor}"
                           Text="Select the item(s) you want to install and click on the 'Install Selected Items' button to start the installation.&#x0a;&#x0a;Use the Search Box to find items quickly.&#x0a;The Save and Import Config buttons in the main window help you manage your preferred selections.&#x0a;&#x0a;Tip: Install UniGetUI from the 'Other Utilities' group if you require a Software Manager as nonsense only provides external app installations, not updates and management."/>
                </Grid>
            </Grid>
        </ScrollViewer>
    </Border>
</UserControl>
