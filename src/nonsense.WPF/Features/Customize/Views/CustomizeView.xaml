<UserControl x:Class="nonsense.WPF.Features.Customize.Views.CustomizeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:nonsense.WPF.Features.Customize.ViewModels"
             xmlns:commonControls="clr-namespace:nonsense.WPF.Features.Common.Controls"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             d:DataContext="{d:DesignInstance Type=viewModels:CustomizeViewModel}"
             mc:Ignorable="d">


    <Grid>
        <!-- Header -->
        <Grid Height="80"
              VerticalAlignment="Top"
              ClipToBounds="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/> <!-- Icon -->
                <ColumnDefinition Width="*"/> <!-- Title and Status -->
                <ColumnDefinition Width="Auto"/> <!-- Search Box -->
            </Grid.ColumnDefinitions>
            
            <!-- Icon -->
            <iconPacks:PackIconMaterial Grid.Column="0"
                                     Kind="Palette"
                                     Width="60"
                                     Height="60"
                                     Margin="10,0,10,0"
                                     VerticalAlignment="Center"
                                     Foreground="{DynamicResource PrimaryTextColor}"/>

            <!-- Title and Status with Quick Nav inline -->
            <StackPanel Grid.Column="1"
                      VerticalAlignment="Center">
                <TextBlock Height="35"
                           VerticalAlignment="Top"
                           FontFamily="Helvetica Neue"
                           FontSize="32"
                           FontWeight="Bold"
                           Foreground="{DynamicResource PrimaryTextColor}"
                           Text="Customizations"/>

                <StackPanel Orientation="Horizontal" 
                            Margin="0,5,0,0">
                    <TextBlock Height="22"
                               VerticalAlignment="Center"
                               FontFamily="Helvetica Neue"
                               FontSize="14"
                               Foreground="{DynamicResource SubTextColor}"
                               Text="{Binding StatusText}"/>
                    
                    <!-- Quick Nav -->
                    <commonControls:QuickNavControl/>
                </StackPanel>
            </StackPanel>

            <!-- Search Box (prioritized) -->
            <Grid Grid.Column="2"
                  HorizontalAlignment="Right"
                  VerticalAlignment="Bottom"
                  Margin="10,0,10,3"
                  Panel.ZIndex="100"
                  MinWidth="200">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <!-- Search Box -->
                <commonControls:SearchBox Grid.Column="0"
                                         Width="200"
                                         HorizontalAlignment="Right"
                                         VerticalAlignment="Center"
                                         SearchText="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                         Margin="0,0,0,1"/>
            </Grid>
        </Grid>

        <!-- Main Content -->
        <Border Margin="0,85,0,0"
                Background="{DynamicResource MainContainerBorderBrush}"
                CornerRadius="10">
            <commonControls:ResponsiveScrollViewer Style="{StaticResource ModernScrollViewer}"
                                                  commonControls:ResponsiveScrollViewer.ScrollPositionCommand="{Binding UpdateScrollPositionCommand}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0"
                            Margin="10">
                        
                        <!-- Dynamically created feature Views with proper DI -->
                        <ItemsControl ItemsSource="{Binding FeatureViews}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ContentPresenter Content="{Binding}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        
                    </StackPanel>

                    <!-- No Search Results Message -->
                    <TextBlock Grid.Row="1"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Margin="0,50,0,20"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               FontSize="16"
                               Foreground="{DynamicResource SubTextColor}"
                               Text="No settings found matching your search. Try different keywords."
                               Visibility="{Binding HasSearchResults, Converter={StaticResource InverseBooleanToVisibilityConverter}}"/>
                </Grid>
            </commonControls:ResponsiveScrollViewer>
        </Border>
    </Grid>
</UserControl>