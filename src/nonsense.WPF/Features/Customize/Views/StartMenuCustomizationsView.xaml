<UserControl x:Class="nonsense.WPF.Features.Customize.Views.StartMenuCustomizationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:nonsense.WPF.Features.Customize.ViewModels"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             d:DataContext="{d:DesignInstance Type=viewModels:StartMenuCustomizationsViewModel}"
             mc:Ignorable="d"
             Visibility="{Binding HasVisibleSettings, Converter={StaticResource BooleanToVisibilityConverter}}">

    <StackPanel>
        <!-- Collapsible Header -->
        <ContentControl Template="{StaticResource CollapsibleHeaderTemplate}"/>
        
        <!-- Content Section -->
        <ContentControl Template="{StaticResource ContentSectionTemplate}"/>
    </StackPanel>
</UserControl>