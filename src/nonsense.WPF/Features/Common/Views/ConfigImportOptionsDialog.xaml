<Window x:Class="nonsense.WPF.Features.Common.Views.ConfigImportOptionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:nonsense.WPF.Features.Common.Views"
        xmlns:controls="clr-namespace:nonsense.WPF.Features.Common.Controls"
        xmlns:converters="clr-namespace:nonsense.WPF.Features.Common.Converters"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="Import Config"
        Height="450"
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="Transparent"
        Foreground="{DynamicResource PrimaryTextColor}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
        <converters:BooleanToThemeIconConverter x:Key="BooleanToThemeIconConverter" />

        <Style x:Key="OptionButtonStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="{DynamicResource ButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Background" Value="{DynamicResource ContentSectionBorderBrush}" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Effect" Value="{StaticResource LightShadowEffect}" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{DynamicResource MainContainerBorderBrush}" />
                    <Setter Property="BorderBrush" Value="{DynamicResource AccentColor}" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource DialogButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="1"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="Red" />
                                <Setter TargetName="border" Property="BorderBrush" Value="Red" />
                                <Setter Property="Foreground" Value="White" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="DarkRed" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.OpacityMask>
            <VisualBrush>
                <VisualBrush.Visual>
                    <Border Background="Black"
                            CornerRadius="10"
                            Width="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=Grid}}"
                            Height="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Grid}}"/>
                </VisualBrush.Visual>
            </VisualBrush>
        </Grid.OpacityMask>

        <Border CornerRadius="10"
                BorderBrush="Transparent"
                Background="{DynamicResource WindowBackground}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header with Title and Close Button -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="1"
                            Style="{StaticResource WindowCloseButton}"
                            Click="CloseButton_Click"
                            Width="32"
                            Height="32"
                            Margin="0,0,0,0"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Top">
                        <iconPacks:PackIconMaterial Kind="Close"
                                                     Width="12"
                                                     Height="12"/>
                    </Button>
                </Grid>
            
            <!-- Dialog Content -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Options -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Import My Own Config Option -->
                    <Border Grid.Column="0"
                            BorderBrush="Transparent" 
                            Margin="10,0,10,0"
                            Style="{StaticResource OptionButtonStyle}"
                            MouseLeftButtonDown="ImportOwnConfig_Click">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <iconPacks:PackIconMaterial Kind="FolderOpen"
                                                   Grid.Row="0"
                                                   Width="125"
                                                   Height="125"
                                                   Margin="0,20,0,5"
                                                   HorizontalAlignment="Center"
                                                   Foreground="{DynamicResource PrimaryTextColor}"/>
                            
                            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Import my own config"
                                           FontWeight="SemiBold"
                                           FontSize="18"
                                           TextAlignment="Center"
                                           Foreground="{DynamicResource PrimaryTextColor}"/>
                                <TextBlock Text="Select a configuration file from your computer"
                                           FontSize="14"
                                           Margin="0,8,0,0"
                                           TextWrapping="Wrap"
                                           TextAlignment="Center"
                                           Foreground="{DynamicResource SecondaryTextColor}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- Import Recommended Config Option -->
                    <Border Grid.Column="1"
                            BorderBrush="Transparent"
                            Margin="10,0,10,0"
                            Style="{StaticResource OptionButtonStyle}"
                            MouseLeftButtonDown="ImportRecommendedConfig_Click">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Image Grid.Row="0"
                                   Width="130"
                                   Height="130"
                                   Margin="0,20,0,5"
                                   HorizontalAlignment="Center"
                                   Source="{Binding Source={x:Static Application.Current},
                                                    Path=Resources[IsDarkTheme],
                                                    Converter={StaticResource BooleanToThemeIconConverter},
                                                    ConverterParameter='pack://application:,,,/Resources/AppIcons/nonsense-white-transparent-bg.ico|pack://application:,,,/Resources/AppIcons/nonsense-black-transparent-bg.ico'}"
                                   RenderOptions.BitmapScalingMode="HighQuality"/>

                            <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                                <TextBlock Text="Import recommended config"
                                           FontWeight="SemiBold"
                                           FontSize="18"
                                           TextAlignment="Center"
                                           Foreground="{DynamicResource PrimaryTextColor}"/>
                                <TextBlock Text="Load nonsense's recommended config file"
                                           FontSize="14"
                                           Margin="0,8,0,0"
                                           TextWrapping="Wrap"
                                           TextAlignment="Center"
                                           Foreground="{DynamicResource SecondaryTextColor}"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
        
                <!-- Buttons -->
                <StackPanel Grid.Row="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            Margin="0,15,0,0">
                    <Button Content="Cancel"
                            Style="{StaticResource CancelButtonStyle}"
                            Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
    </Grid>
</Window>
